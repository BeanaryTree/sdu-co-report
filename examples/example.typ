#import "../report.typ": *

#show: experiment-report.with(
  college: "计算机科学与技术",
  course: "计算机组成原理",
  student-id: "202500000000",
  name: "张三",
  class: "计科2025级1班",
  title: "移位电路实验",
  hours: 2,
  date: "2026-09-23",
)

#section("实验目的")[
  学会并且掌握采用传送方式实现二进制数的移位电路。
]

#section("实验软件和硬件环境")[
  *软件环境：*

  Vivado 软件、FPGA 实验平台

  *硬件环境：*

  + 实验室台式机
  + FPGA 服务器，PYNQ-Z2 开发板
]

#section("实验原理和方法（含有实验示意图）")[
  移位电路根据控制信号，将输入数据传送到相邻位。逻辑左移时，最低位补零；逻辑右移时，最高位补零。

  #align(center)[#image("assets/shift.svg", width: 60%)]
]

#section("实验步骤")[
  + 在 Vivado 中建立工程，编写移位逻辑并连接输入、输出及控制信号。
  + 设置仿真激励，分别验证保持、左移和右移操作。
  + 完成管脚约束、综合与实现，将设计下载到 FPGA 开发板。
  + 改变输入数据和移位方向，记录输出并与预期结果比较。

  以下是记录格式示例，请用实际实验结果替换：

  #three-line-table(
    header: ([操作], [输入], [预期输出]),
    columns: (1.2fr, 1fr, 1fr),
    [逻辑左移一位], [0010 1101], [0101 1010],
    [逻辑右移一位], [0010 1101], [0001 0110],
  )
]

#section("结论分析与体会")[
  在此填写实测结果、与理论值的比较、遇到的问题及解决方法。
]
